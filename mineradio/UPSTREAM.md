# Historical Mineradio attribution

The vendored upstream snapshot is retained for license and migration
attribution only. It is not served by Elysium. Production playback is the
native React implementation under `frontend/src/features/music/`, with direct
provider adapters under `backend/music/`.
